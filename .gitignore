sdkconfig
sdkconfig.mqtt
sdkconfig.old
build/
managed_components/
build_http/
sdkconfig.httpcheck
.claude/
.gemini/
.vault-portal.conf

# Vault + skill vault dung chung - symlink per-machine, source o repo rieng
.obsidian-vault
.common-skill
.esp32-skill

# Serena MCP local cache/config (per-machine)
.serena/

# graphify artifact - per-may, dung lai duoc bang `graphify update .`
graphify-out/

# CLAUDE.md/AGENTS.md = symlink per-machine -> sync qua claude-agents
CLAUDE.md
AGENTS.md

# compile_commands.json o root = symlink -> build/compile_commands.json,
# tao thu cong de clangd/Serena resolve include ESP-IDF that (per-machine,
# path tuyet doi ben trong khac nhau giua may)
/compile_commands.json
